// @lovable.dev/vite-tanstack-config already includes everything this project needs —
// tanstackStart, viteReact, tailwindcss, tsConfigPaths, componentTagger (dev-only),
// VITE_* env injection, @ path alias, React/TanStack dedupe, and sandbox detection.
// Do NOT add these manually or the app will break with duplicate plugins.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({});
